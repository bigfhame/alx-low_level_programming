#include "main.h"
#include <string.h>

/**
 *is_palindrome - function to check
 *@*s: string to check
 *
 *Return: a string
 */

int is_palindrome(char *s)
{
	if (strlen(s) == 0)
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
