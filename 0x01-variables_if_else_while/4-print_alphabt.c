#include <stdio.h>
#include <stdlib.h>
/**
*main - Entry point
*
*Return: always 0
*/
int main(void)
{
char lc, llc, bj;
char co = lc && llc && bj;
for (lc = 'a'; lc < 'e'; lc++)
{
for (llc = 'f'; llc < 'q'; llc++)
{
}
putchar(co);
}
putchar('\n');
return (0);
}
