#include "main.h"
#include <stdio.h>
/**
*main - check for lowercase character
*
*Return: always 0
*/
int _islower(int c);
int main(void)
{
c = 'H';
if (_islower(c))
{
	_putchar(1);
}
else
{
_putchar(0);
}
return (0);
}
