#include "main.h"
#include <stdio.h>
/**
*_islower - check for lowercase character
*
*Return: always 0
*/
int _islower(int c)
{
char low;
low = _islower(c);
low = _islower('o');
_putchar(low + '0');
low = _islower(108);
_putchar(low + '0');
return (0);
}
