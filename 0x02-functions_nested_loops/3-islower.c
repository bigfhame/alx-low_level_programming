#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
*_islower - check for lowercase character
*@c: character to check
*Return: always 0
*/
int _islower(int c)
{
c = 1;
if (c > 0)
{
return (c);
}
else
{
return (0);
}
}
