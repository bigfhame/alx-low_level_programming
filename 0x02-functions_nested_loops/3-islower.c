#include "main.h"
#include <stdio.h>
/**
*_islower - check for lowercase character
*int c - character to be accessed
*Return: always 0
*/
int _islower(int c)
{
	int c = 'H';
if (_islower(c))
{
	_putchar(1);
}
else
{
_putchar(0);
}
return (0);
}
